{
	"name": "mediq",
	"version": "0.0.0",
	"main": "index.js",
	"repository": "git@github.com:harryparkdotio/mediq.git",
	"author": "Harry Park <harry@harrypark.io>",
	"license": "MIT",
	"scripts": {
		"build": "tsc -p tsconfig.build.json",
		"cover": "codecov",
		"test": "yarn test:lint && yarn test:types && yarn test:unit",
		"test:lint": "tslint -t codeFrame '{src,test}/**/*.ts'",
		"test:types": "tsc",
		"test:unit": "jest --runInBand"
	},
	"devDependencies": {
		"@types/jest": "^23.3.2",
		"codecov": "^3.1.0",
		"jest": "^23.6.0",
		"ts-jest": "^23.10.1",
		"tslint": "^5.11.0",
		"typescript": "^3.0.3"
	}
}
